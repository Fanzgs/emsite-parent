<?xml version="1.0" encoding="utf-8"?>
<template>
	<name>facadeServiceImpl</name>
	<filePath>src/main/java/${packageName}/${moduleName}/facade/impl/${subModuleName}</filePath>
	<fileName>${ClassName}FacadeServiceImpl.java</fileName>
	<content><![CDATA[
/**
 * Copyright &copy; 2017 <a href="https://gitee.com/hackempire/emsite-parent">emsite</a> All rights reserved.
 */
package ${packageName}.${moduleName}.facade.impl<#if subModuleName != "">.${subModuleName}</#if>;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.empire.emsite.common.persistence.Page;
import ${packageName}.${moduleName}.entity<#if subModuleName != "">.${subModuleName}</#if>.${ClassName};
import ${packageName}.${moduleName}.service<#if subModuleName != "">.${subModuleName}</#if>.${ClassName}Service;
import ${packageName}.${moduleName}.facade<#if subModuleName != "">.${subModuleName}</#if>.${ClassName}FacadeService;

/**
 * ${functionName}FacadeServiceImpl
 * @author ${functionAuthor}
 * @version ${functionVersion}
 */

/**
 * 类${ClassName}FacadeServiceImpl.java的实现描述：${functionName}FacadeService接口实现类
 * 
 * @author ${functionAuthor} ${functionDate}
 */
@Service
public class ${ClassName}FacadeServiceImpl implements ${ClassName}FacadeService {
	@Autowired
    private ${ClassName}Service ${className}Service;
	
	public ${ClassName} get(String id) {
		return ${className}Service.get(id);
	}
	
	public List<${ClassName}> findList(${ClassName} ${className}) {
		return ${className}Service.findList(${className});
	}
	
	
	public void save(${ClassName} ${className}) {
		${className}Service.save(${className});
	}
	
	public void delete(${ClassName} ${className}) {
		${className}Service.delete(${className});
	}
	
}]]>
	</content>
</template>